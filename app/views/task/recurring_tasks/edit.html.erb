<h1 class="text-2xl font-bold mb-4"><%= t("task.recurring_tasks.edit.title") %></h1>

<%= render "form", recurring_task: @recurring_task, task_areas: @task_areas %>

<p class="mt-4">
  <%= link_to t("shared.back"), task_recurring_tasks_path, class: "text-blue-600 hover:underline" %>
</p>
