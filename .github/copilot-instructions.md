## Code Style
- use Tailwind CSS for styling

## Testing
- Write tests using default Rails testing library "Minitest", do include fixtures.

## Naming Conventions
- use compact namespace syntax with :: for class definitions, example 'MyModule::OtherModule::HelloService'
- Every class must follow rails conventions, therefore must end with a suffix describing its role (Service, Query, Controller and any other rails suffix, etc.)
